type: object
required:
  - id
  - type
properties:
  type:
    type: string
    description: The type of the resource
    enum:
      - authTokens
    example: authTokens
  id:
    type: string
    description: The unique identifier for the resource
    example: 1