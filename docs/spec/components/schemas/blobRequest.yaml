allOf:
  - $ref: '#/components/schemas/blobKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - value
        properties:
          value:
            description: arbitrary text
            type: string
            example: foobar
    headers:
      Authorization:
        type: string
